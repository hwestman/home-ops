---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: alertmanager-ntfy
spec:
  healthChecks:
    - apiVersion: apps/v1
      kind: Deployment
      name: alertmanager-ntfy
      namespace: observability
  interval: 1h
  path: ./kubernetes/apps/observability/alertmanager-ntfy/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  targetNamespace: observability
  wait: false
