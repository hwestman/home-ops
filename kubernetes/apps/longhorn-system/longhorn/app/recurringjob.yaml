---
apiVersion: longhorn.io/v1beta2
kind: RecurringJob
metadata:
  name: filesystem-trim
  namespace: longhorn-system
spec:
  cron: "0 4 * * *"
  task: "filesystem-trim"
  retain: 1
  concurrency: 1
  groups:
    - default
